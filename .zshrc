# my alias
alias cdp="cd /var/mplus/TanukiChat"

# Path
export PATH=$PATH:/usr/local/Cellar/scala/2.11.1/bin

# LANG
export LANG=ja_JP.UTF-8

# Default Alias 
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias ......="cd ../../../../.."

alias la="ls -a"
alias lf="ls -F"
alias ll="ls -l"

alias du="du -h"
alias df="df -h"

alias su="su -l"

# Install Oh My Zsh and Add aliases above this line :)
# curl -L http://install.ohmyz.sh | sh
# wget --no-check-certificate http://install.ohmyz.sh -O - | sh
